---
description: After substantive code changes, commit and push so Vercel/GitHub can deploy
alwaysApply: true
---

# 完成后推送到远程（上线）

当在 **本仓库（LEE Wealth）** 完成**一批有意义的修改**（功能/修复/文案等，而非仅临时试验）时：

1. 用 `git status` 确认要纳入的变更；不要提交密钥、`.env`、`functions/node_modules`、仅本地的垃圾文件。
2. `git add` 相关文件后 `git commit -m "…"`，说明用简短中文或英文完整句。
3. **推送到默认分支**（与 Vercel 绑定的分支，一般为 `main`）：
   - 若本地分支无上游、远程默认名为 `main`：`git push -u origin HEAD:main` 或 `git push origin HEAD:main`
   - 若已设置上游为 `origin/main`：`git push` 即可。
4. 若 `git push` 因认证失败，向用户说明需在终端完成登录或配置凭据，不要反复无意义重试。
5. **推送成功后在本对话中简要告知用户**（例如：已 push、commit 摘要、是否触发了 GitHub Actions 通知）。

不要为极小的、用户明确表示「先别提交」的改动自动 push。

若本目录**尚未** `git init` 或未配置 `origin`，应先请用户创建 GitHub 仓库并执行 `git remote add origin …`，再执行 push。
